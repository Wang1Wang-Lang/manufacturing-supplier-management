---
name: manufacturing-supplier-management
description: 面向制造业的供应商管理专家技能。当用户提到「供应商准入」「供应商评估」「采购比价」「品类策略」「Kraljic」「支出分析」「招投标」「招标方式」「should-cost」「目标成本」「绩效」「配额」「份额」「PIP」「改进计划」「供应商打分」「比价单」「供应商风险核查」「风险监控」「台账」「三单匹配」「采购执行」「PR/PO」「收货」「对账」「付款」「审批权限」「合规」「利益冲突」「采购KPI」「支出集中度」「HHI」「单源」「数据接入」「ERP导出」「SRM」等关键词时触发。覆盖「品类策略(Kraljic) → 供应商准入 → 供应商评估 → 采购比价 → 招投标与成本拆解 → 绩效分级与配额 → 公开风险核查 → 持续风险监控 → 采购执行闭环(三单匹配) → 合规审批与KPI → 数据接入适配」十一步闭环，评估聚焦四大维度——质量体系、产能、交付准时率、成本，输出固定表格模板，并能自动核查供应商的公开风险信息（失信、经营异常、行政处罚、股权冻结、裁判文书、破产等）。仅依赖 Python 标准库，无需安装任何包。
risk: low
source: community
---

# 制造业供应商管理专家（Manufacturing Supplier Management）

> 本技能为本地自建技能，面向制造业采购/供应链场景，提供「品类策略(Kraljic) → 供应商准入 → 供应商评估 → 采购比价 → 招投标与成本拆解 → 绩效分级与配额 → 公开风险核查 → 持续风险监控 → 采购执行闭环(三单匹配) → 合规审批与KPI → 数据接入适配」十一步闭环。所有评估输出均按固定表格模板，便于横向对比与归档。

## 触发识别

当用户意图命中以下任一类，即进入本技能工作流：

| 触发词 | 对应工作流 |
|---|---|
| 供应商准入、供应商引入、新供应商审核、准入评估、供应商开发审核 | **工作流一：供应商准入** |
| 供应商评估、供应商打分、供应商考核、季度/年度评估、供应商评级 | **工作流二：供应商评估** |
| 采购比价、比价单、询价对比、多家报价对比、选哪家供应商 | **工作流三：采购比价** |
| 供应商风险、公开风险核查、背景调查、信用核查、老赖/失信核查 | **工作流四：公开风险核查** |
| 品类策略、品类管理、Kraljic、物料分类、支出分析、战略寻源 | **工作流五：品类策略（Kraljic 矩阵）** |
| 招投标、招标方式、比价方式、should-cost、目标成本、成本拆解、报价分析 | **工作流六：招投标与成本拆解** |
| 绩效、配额、份额分配、PIP、改进计划、分级管理、月度考核、QCDS | **工作流七：绩效分级与配额（PIP 闭环）** |
| 风险监控、风险台账、持续监控、定期核查、重查、风险看板 | **工作流八：持续风险监控（台账化）** |
| 采购执行、PR/PO、收货、IQC、三单匹配、对账、付款、执行闭环 | **工作流九：采购执行闭环（三单匹配）** |
| 审批权限、合规、利益冲突、采购KPI、支出集中度、HHI、单源、降本 | **工作流十：合规审批与 KPI 看板** |
| 数据接入、ERP导出、SRM、导入映射、标准化、适配器 | **工作流十一：数据接入适配（离线）** |

> 若同时涉及多个，按「品类策略 → 准入 → 评估 → 比价 → 招投标与成本拆解 → 绩效分级与配额 → 风险核查 → 持续监控 → 执行闭环 → 合规KPI → 数据接入」顺序串起来做；若只问其一，只跑对应工作流。

## 你的角色与原则

- 你是制造业供应商管理专家，说话带数据、给结论、附证据。
- 评估**只盯四大维度**：质量体系、产能、交付准时率、成本（风险作为扣分项与一票否决项）。**注**：准入环节在四维基础上增设「资质合规」前置一票否决项（见工作流一），故准入评估表为五项、评估报告为四项。
- 所有交付物**必须套用固定表格模板**（见下方「本地附属资源」），不要自由发挥格式。
- 凡涉及「是否准入」「是否独家」「评级高低」，必须给出**明确的量化依据**，不靠感觉。

## 本地附属资源

本技能自带可直接使用的文件资源，触发对应工作流时应优先调用，不要凭空编造表格：

- `references/供应商管理表格模板.xlsx`：固定表格模板，含 4 个工作表——「供应商准入评估表」「供应商评估报告」「采购比价表」「公开风险核查表」。让用户在此维护底数，或由本技能按模板生成评估结果。
- `scripts/risk_check.py`：公开风险核查辅助脚本。输入供应商名称（及统一社会信用代码），输出**要执行的核查搜索清单**与**空白风险核查表（markdown）**，支持单条与批量 CSV。运行：`python risk_check.py --name "供应商全称" [--credit-code "91xxxxxxxx"]`，或 `python risk_check.py --batch suppliers.csv --out queries.md`。
- `references/品类策略模板.xlsx`：品类管理模板，含 3 个工作表——「Kraljic矩阵」（填入品类自动定位 + 给策略）、「品类策略表」（按定位做差异化策略管理）、「支出分析ABC」（帕累托分类，标 A/B/C）。建议**先做品类定位，再据定位决定准入/评估/比价的打法**。
- `scripts/kraljic_classify.py`：品类 Kraljic 定位分类脚本。输入年采购金额与供应风险(高/低)，输出定位与标准策略，支持单条与批量 CSV。运行：`python kraljic_classify.py --spend 1200 --risk 高 [--threshold 500]`，或 `python kraljic_classify.py --batch categories.csv --out result.md`。
- `references/招标比价与成本拆解模板.xlsx`：含 3 个工作表——「招投标方式选择」（填入品类定位/金额/供应商数/紧急度自动推荐公开招标/邀请招标/竞争性谈判/询价/单一来源）、「Should-Cost成本拆解」（逐项填成本要素自动算目标成本与报价差异率，红黄绿预警）、「成本要素参数说明」。做大宗/战略采购前先用它算 should-cost 再谈价。
- `scripts/should_cost.py`：Should-Cost 成本拆解与招投标方式推荐脚本（纯标准库）。子命令 `cost`（成本要素串/批量 CSV + 报价 → 差异率预警）与 `tender`（定位/金额/供应商数/紧急度 → 推荐方式）。运行：`python should_cost.py cost --elements "直接材料:1200,直接人工:300,制造费用:200,专项费用:150,管理费:200,利润:250" --quote 2300`，或 `python should_cost.py tender --position 杠杆型 --amount 80 --suppliers 4 --urgency 中`，批量加 `--batch file.csv --out result.md`。
- `references/绩效配额与改进模板.xlsx`：含 3 个工作表——「供应商分级与配额」（填评估等级→自动给角色/建议份额/主供备选，含份额合计校验）、「PIP改进计划」（问题→根因→措施→责任→期限→验收→闭环，逾期自动标红、已完成标绿）、「QCDS绩效看板」（质量/成本/交付/服务四维 1-5 分→加权综合分与等级，A 绿 D 红）。做周期考核与供应商池管理时用。
- `scripts/perf_plan.py`：绩效与配额辅助脚本（纯标准库）。子命令 `grade`（Q/C/D/S 四维 1-5 分→综合分与等级）、`quota`（供应商:等级 串/批量 CSV→归一化份额分配与角色）、`pip`（问题清单 CSV→PIP 改进计划表）。运行：`python perf_plan.py grade --q 5 --c 4 --d 5 --s 4`，或 `python perf_plan.py quota --grades "供应商A:A,供应商B:B,供应商C:C,供应商D:D"`，PIP：`python perf_plan.py pip --batch issues.csv --out pip.md`。
- `references/风险监控台账.xlsx`：含 2 个工作表——「风险监控台账」（供应商×12 监控项逐行登记，自动算距今天数/需重查/>90天标红/下次核查日期）、「风险汇总看板」（按供应商聚合高风险项数、最久未查天数、整体状态）。做持续风控时用。
- `scripts/risk_monitor.py`：持续风险监控脚本（纯标准库）。单条 `--supplier/--date/--result/--item/--source` 试算；批量 `--batch ledger.csv --out dashboard.md` 按台账计算时效、需重查(>90天)、按供应商汇总整体风险。运行：`python risk_monitor.py batch --batch ledger.csv --out dashboard.md`。
- `references/采购执行闭环.xlsx`：含 3 个工作表——「PRPO」（请购/订单登记，PO金额自动算）、「IQC」（收货与检验，不良率自动算）、「三单匹配」（PO×收货×发票自动取数，差异=发票−应付基准，匹配状态=通过/异常 自动标色）。做订单执行与对账时用。
- `scripts/po_tracker.py`：采购执行闭环脚本（纯标准库）。子命令 `match`（CSV: po,recv_qty,unit_price,invoice_amount,tol → 三单匹配状态）、`delivery`（CSV: po,plan_date,actual_date,recv_qty,ok_qty → 准时/延迟、不良率）。运行：`python po_tracker.py match --batch gr.csv --out match.md`。
- `references/合规审批与KPI.xlsx`：含 3 个工作表——「审批授权矩阵」（输入采购金额自动判定审批层级 CEO/副总/总监/经理）、「合规声明」（利益冲突申报表）、「采购KPI看板」（年降本/支出集中度HHI/单源占比/准时率/合格率/风险覆盖率，达成与状态自动算）。做合规与绩效汇报时用。
- `scripts/kpi_dashboard.py`：采购 KPI 与支出集中度脚本（纯标准库）。子命令 `concentration`（CSV: supplier,amount → HHI、最大供应商占比、单源/高集中预警）、`costdown`（CSV: item,baseline,current,qty → 降本总额与降本率）。运行：`python kpi_dashboard.py concentration --batch spend.csv --out kpi.md`。
- `references/数据接入适配.xlsx`：离线数据接入规范——「导入映射规范」（ERP/SRM 导出列名 → 内部字段映射、类型与校验）、「示例数据」（可被适配器直接读的标准 CSV 样例）。**本技能不直连系统**，用下方脚本把导出数据标准化后喂给其他脚本。
- `scripts/import_adapter.py`：ERP/SRM 数据接入适配器（纯标准库，离线）。`validate --file erp.csv` 校验导出列；`normalize --file erp.csv --out std.csv`（支持 .json）按映射规范标准化为内部格式。运行：`python import_adapter.py normalize --file erp.csv --out std.csv`。

> 说明：脚本本身不能联网，它的作用是**生成标准核查项与搜索词 + 空白核查表**；实际联网核查由你（Agent）按脚本产出的搜索词逐项执行 WebSearch，并把结果回填到核查表中。

---

## 工作流一：供应商准入

**目标**：判断一个新供应商能不能进合格供应商名录。

**步骤**：
1. 收集供应商基础资料（名称、统一社会信用代码、主营品类、地区、联系人、产能规模、资质证书）。
2. 跑一次「工作流四：公开风险核查」做前置排雷（有重大风险直接一票否决，不进入后续评分）。
3. 按 `references/供应商管理表格模板.xlsx` →「供应商准入评估表」逐项打分：
   - **资质合规**（一票否决项）：营业执照、体系证书（ISO9001/IATF16949 等）、行业强制认证、环保/安监合规。
   - **质量体系**：检验设备、来料/过程/出货管控、不良率基线、客诉机制。
   - **产能**：关键设备与产线、月/年产能、产能余量（≥20% 为佳）、扩产柔性。
   - **交付**：历史准时率、交期稳定性、急单响应、物流方案。
   - **成本**：报价合理性、TCO（含质量损失/切换成本）、降价空间。
4. 计算准入总分（权重见模板），分级：A≥85 / B 70–84 / C 60–69 / D<60。
5. 给出结论：**准入 / 附条件准入（列出条件）/ 不准入（列出硬伤）**。

**输出**：套用「供应商准入评估表」模板，并附一句结论。

---

## 工作流二：供应商评估（四维加权）

**目标**：对已在合作名单中的供应商做周期考核与分级。

**评估模型（100 分制）**：

| 维度 | 权重 | 核心指标 |
|---|---|---|
| 质量体系 | 30% | 体系认证等级、过程能力(CPK)、来料合格率、质量事故次数、8D/CAPA 闭环 |
| 产能 | 25% | 设备稼动率、产能余量、瓶颈工序、扩产响应 |
| 交付准时率 | 25% | 准时交付率、交期波动(σ)、急单履约、断料次数 |
| 成本 | 20% | 报价竞争力、TCO、年降达成、对账与开票合规 |

**步骤**：
1. 取该供应商近一期数据（合格率、准时率、产能、单价、事故等）。
2. 逐项给 1–5 分（5=优秀），加权得分 = Σ(评分 × 权重)。
3. 叠加风险扣分（来自工作流四，重大风险可一票否决）。
4. 定级：A≥85 / B 70–84 / C 60–69 / D<60；C 级发黄牌、D 级启动淘汰或帮扶。
5. 输出改进项（每条问题 → 责任 → 期限）。

**输出**：套用「供应商评估报告」模板，结论 + 下期重点。

---

## 工作流三：采购比价

**目标**：在 ≥2 家供应商间做科学比价与选型。

**比价原则**：不只看单价，要看**加权单价 = 含税单价 ÷ 质量系数**，质量系数由历史合格率/评级折算（如 A=1.0、B=0.97、C=0.93、D=0.88，有质量事故再下调）。

**字段**（见「采购比价表」）：物料、供应商、不含税单价、含税单价(自动=不含税×(1+税率))、MOQ、交期(天)、账期、质量系数、加权单价、综合排名、备注。

**步骤**：
1. 收集各家报价与商务条件（单价、MOQ、交期、账期、税率、历史质量）。
2. 折算质量系数，算加权单价。
3. 综合排名，给出推荐（可主供+备选组合，避免单源）。
4. 提示单源/高集中风险（某家占比过高或仅 1 家可选）。

**输出**：套用「采购比价表」模板，附推荐结论与风险提示。

---

## 工作流四：公开风险核查

**目标**：用公开信息核查供应商的经营与信用风险，作为准入/评估的前置排雷。

**标准核查项（逐项执行 WebSearch 并回填）**：
1. 失信被执行人（中国执行信息公开网）
2. 限制消费令
3. 经营异常名录（国家企业信用信息公示系统）
4. 行政处罚（市场监管/环保/安监/税务）
5. 股权冻结 / 司法冻结
6. 裁判文书（买卖合同纠纷、欠款）
7. 破产重整 / 破产清算
8. 环保/安全生产处罚
9. 欠税公告
10. 劳动仲裁 / 社保欠缴
11. 招投标黑名单 / 严重违法失信名单
12. 重大负面舆情

**步骤**：
1. 调用 `scripts/risk_check.py` 生成「搜索词清单 + 空白核查表」。
2. 对每一项搜索词执行 WebSearch，记录结果（无 / 有）、风险等级（无/低/中/高）、详情与来源链接、核查日期。
3. 汇总：出现「失信/限高/破产/大额股权冻结/重大处罚」任一即判**高风险**，准入一票否决；多项中风险合并判中风险。
4. 输出「公开风险核查表」。

**判定规则**：
- 高风险（一票否决）：失信被执行人、限制消费令、破产、股权冻结、重大行政处罚。
- 中风险：经营异常、多起买卖纠纷、劳动仲裁、欠税。
- 低风险：零星纠纷、已整改的异常。
- 无风险：以上均查无。

---

## 工作流五：品类策略（Kraljic 矩阵）

**目标**：在动手准入/评估/比价之前，先按品类定位决定「怎么打」——战略型、杠杆型、瓶颈型、非关键型的资源投入与管控强度完全不同。这是后续所有工作流的**前置战略开关**。

**Kraljic 二维模型**：
- X 轴 = 采购金额（高 / 低）
- Y 轴 = 供应风险（高 / 低：稀缺性、替代难度、供应商集中度、断料影响）
- 四象限：战略型（金额高+风险高）、杠杆型（金额高+风险低）、瓶颈型（金额低+风险高）、非关键型（金额低+风险低）。

**标准策略（自动给出）**：
- **战略型**：建立战略伙伴、联合开发/VAVE、双源备份、高层定期回顾、JIT/VMI 协同。
- **杠杆型**：集中采购、招标比价压价、标准化、多家竞争、年降目标、合同价锁定期。
- **瓶颈型**：多源/替代料开发、安全库存、供应商扶持、长期保供协议、动态风险监控。
- **非关键型**：流程简化、系统/电商化采购、缩减供应商数、低管理成本、按需采购。

**步骤**：
1. 调用 `scripts/kraljic_classify.py` 对每个品类做定位（输入年采购金额 + 供应风险），或直接在 `references/品类策略模板.xlsx` →「Kraljic矩阵」表填入品类自动定位。
2. 把定位结果落到「品类策略表」，填写目标供应商数、寻源/定价/库存/风险缓解策略、负责人与复盘周期。
3. 跑「支出分析ABC」做帕累托：年支出降序排列后，累计 ≤80% 为 A 类（重点管控）、≤95% 为 B 类、其余 C 类。
4. 据定位驱动态度：**战略/瓶颈型 → 准入与评估从严、必须有备份源；杠杆型 → 比价压价为主；非关键型 → 简化流程、降低管理成本**。

**输出**：套用「品类策略表」+「支出分析ABC」两张表，结论须标注每个品类的 Kraljic 定位与对应策略。

---

## 工作流六：招投标与成本拆解（Should-Cost）

**目标**：在比价/谈判前先算清「这个零件/物料到底该值多少钱」，并据品类定位与商务条件选对采购方式，避免被报价牵着走。这是对「工作流三：采购比价」的**前置强化**——比价不只看谁便宜，更看谁接近真实成本。

**Why**：纯比价（工作流三）只能横向排高低，无法判断「报价本身是否虚高」。Should-Cost 用材料/人工/制造/专项/管理/利润逐项拆出目标成本，直接暴露报价的虚高空间；招投标方式则把采购合规与效率前置。

**Should-Cost 拆解口径**（对应模板「Should-Cost成本拆解」表）：
- 直接材料 = Σ(零件净重 × 材料单价 × (1+损耗率))
- 直接人工 = Σ(工序标准工时 × 工时费率)
- 制造费用 = 设备折旧/能源/厂房/刀具分摊
- 专项费用 = 模具摊销/外协/运费/包装
- 管理费 = (材料+人工+制造+专项) × 费率(8%~15%)
- 利润 = 成本合计 × 目标利润率(5%~12%)
- **Should-Cost 合计** = 以上求和；**差异率 = (报价 − Should-Cost) ÷ Should-Cost**
- 判定：差异率 <5% 绿（合理）；5%~15% 黄（偏高，可谈）；≥15% 红（严重偏高，须拆解报价/换源）。

**招投标方式推荐规则**（对应模板「招投标方式选择」表，与 `should_cost.py tender` 一致）：
1. 可选供应商 = 1 → **单一来源**（须做成本与合规审查）。
2. 紧急度高 且 ≥2 家 → **竞争性谈判**（快速定标）；紧急但无竞争 → 单一来源(应急)。
3. 战略型品类 → **公开招标**（最大化竞争与透明）。
4. 杠杆型 且 金额≥50万 → **邀请招标**。
5. 瓶颈型 → **竞争性谈判**（锁定供应）。
6. 非关键型 且 金额<50万 → **询价**。
7. 其他默认 → **邀请招标**。

**步骤**：
1. 调用 `scripts/should_cost.py tender` 或模板「招投标方式选择」确定采购方式。
2. 收集供应商报价后，调用 `scripts/should_cost.py cost`（或模板「Should-Cost成本拆解」）逐项填成本要素、输入报价，得到差异率与预警。
3. 差异率偏高（黄/红）时，要求供应商拆分报价、谈判降价，或引入备选/替代料；红档须写入决策依据。
4. 将 should-cost 结论并入比价表（工作流三）作为「成本合理性」维度，形成「加权单价 + should-cost 双视角」决策。

**输出**：套用「Should-Cost成本拆解」表，结论须含 Should-Cost 合计、报价、差异率与预警等级；若涉及采购方式选择，并附推荐方式与理由。

---

## 工作流七：绩效分级与配额（PIP 闭环）

**目标**：把「评估打分」真正落到供应商池的**分级管理、配额分配、持续改进**上——这是采购日常最长做、原技能最缺的一环。评估（工作流二）只给分数，本工作流把分数变成**管理动作**：谁主供、谁淘汰、谁要整改。

**QCDS 绩效口径**（对应模板「QCDS绩效看板」）：
- Q 质量：批次合格率、PPM（百万不良率）、抱怨次数、质量成本 COPQ。
- C 成本：成本竞争力、年降达成。
- D 交付：交付达成率、交期波动。
- S 服务：响应速度、配合度、信息透明。
- 综合分 = (质量×30% + 成本×20% + 交付×25% + 服务×25%) × 20（1–5 分制映射到 100 分），定级 A≥85 / B 70–84 / C 60–69 / D<60。

**供应商分级与配额**（对应模板「供应商分级与配额」）：
- A 级 → 战略/优选：主供，建议份额 50%（战略型可 60–70%），重点维护。
- B 级 → 合格：备选/并行，建议份额 30%。
- C 级 → 限制(帮扶)：份额 ≤15%，**必须启动 PIP**。
- D 级 → 淘汰：份额 0%，寻找替代源。
- 配额须按品类归一化到 **合计 ≈100%**（模板自带校验单元格提示）。

**PIP 改进计划闭环**（对应模板「PIP改进计划」）：问题 → 根因 → 措施 → 责任 → 计划完成 → 验收标准 → 实际完成/闭环证据；状态（进行中/已完成/逾期）逾期自动标红。每条 C/D 级短板都要有对应 PIP，验收通过才算闭环。

**步骤**：
1. 用 `scripts/perf_plan.py grade` 或模板「QCDS绩效看板」算各供应商综合分与等级。
2. 用 `scripts/perf_plan.py quota`（或模板「供应商分级与配额」）据等级分配份额，确认合计≈100%、单源/高集中（某家>50%）须提示。
3. 对 C/D 级供应商，用 `scripts/perf_plan.py pip`（或模板「PIP改进计划」）生成改进计划，明确责任人、期限与验收。
4. 下期复盘：PIP 是否闭环、等级是否改善，未闭环的 C 级转 D 级淘汰流程。

**输出**：套用「供应商分级与配额」+「PIP改进计划」两张表；结论须含每家的等级、角色、建议份额，以及 C/D 级的 PIP 清单与闭环状态。

---

## 工作流八：持续风险监控（台账化）

**目标**：把工作流四的「一次性核查」变成**持续台账**——记录每次核查结果、自动提示过期（>90 天）需重查、按供应商聚合整体风险，避免「查过一次就当永久安全」。

**Why**：公开风险是动态变化的（今天无失信不代表半年后无）。一次性核查的漏洞在于「时间衰减」——台账化 + 重查提醒才能真兜底。

**台账口径**（对应模板「风险监控台账」）：
- 逐行登记：供应商、统一信用代码、监控项（12 项）、最近核查日期、核查结果（无/低/中/高）、信息来源。
- 自动列：距今天数 `=TODAY()-日期`、需重查 `=距今天数>90 → 是`、下次核查日期 `=日期+90`。
- 「风险汇总看板」按供应商聚合：高风险项数（COUNTIFS）、最久未查天数（MAXIFS）、整体状态（高>0→高风险 / 中>0→中风险 / 否则低风险）。

**步骤**：
1. 用工作流四的 `scripts/risk_check.py` 产出搜索词，逐条 WebSearch 后把结果回填到「风险监控台账」。
2. 调用 `scripts/risk_monitor.py batch --batch ledger.csv --out dashboard.md` 生成监控看板（时效、需重查、整体风险）。
3. 对「需重查=是」或整体高风险的供应商，重新执行工作流四核查并更新台账。

**输出**：套用「风险监控台账」+「风险汇总看板」；结论须标注每个供应商的整体风险等级与最久未查天数，标红项须提示重查。

---

## 工作流九：采购执行闭环（三单匹配）

**目标**：把采购从「定好供应商」推进到「订单真正闭环」——PR→PO→收货→IQC→三单匹配（PO/收货/发票）→付款，并用三单匹配自动发现差异与异常。

**Why**：比价/准入只解决「选谁、多少钱」，执行环节的钱货票一致性才是资金安全闸门。本环境不连 ERP，故做成**结构化执行台账 + 校验脚本**，用导出数据跑闭环。

**三单匹配口径**（对应模板「三单匹配」）：
- 应付基准 `= 收货数量 × 单价`（收货数量来自 IQC 表、单价/PO金额来自 PRPO 表，自动 INDEX-MATCH 取数）。
- 差异 `= 发票金额 − 应付基准`；匹配状态 `= |差异| ≤ 容差 → 通过，否则 异常`（容差默认 0.01，可调）。
- 「PRPO」表 PO金额 `=数量×单价`；「IQC」表 不良率 `=1−合格/收货`。

**步骤**：
1. 用工作流十一的 `import_adapter.py` 把 ERP/WMS/QMS 导出标准化为内部格式。
2. 调用 `scripts/po_tracker.py match --batch gr.csv --out match.md` 做三单匹配，输出每单差异与异常清单。
3. 调用 `scripts/po_tracker.py delivery --batch del.csv --out del.md` 看准时/延迟与不良率。
4. 异常单（三单不匹配 / 延迟 / 高不良）挂起付款，转 PIP（工作流七）或退换货。

**输出**：套用「三单匹配」+「IQC」+「PRPO」三表；结论须含异常单数、延迟单数、平均不良率，异常单须显式标红并给出处理动作。

---

## 工作流十：合规审批与 KPI 看板

**目标**：把采购的**合规门槛**与**绩效结果**显性化——审批权限按金额分级、利益冲突申报留痕、用 KPI 看板回答「今年降本多少、供应多集中、质量交付如何」。

**审批授权口径**（对应模板「审批授权矩阵」）：输入采购金额自动判定层级，阈值示例：≥100万→CEO、≥20万→副总、≥5万→总监、其余→经理（可按企业制度改阈值）。

**KPI 口径**（对应模板「采购KPI看板」）：
- 支出集中度 HHI `= Σ(供应商支出占比²)`，>0.25 预警；单源占比 = 唯一供应商支出/总支出。
- 年降本金额/率、准时交付率、来料合格率、风险核查覆盖率，达成与状态自动判达标/超标/未达标。

**步骤**：
1. 用 `scripts/kpi_dashboard.py concentration --batch spend.csv --out kpi.md` 算集中度与单源预警。
2. 用 `scripts/kpi_dashboard.py costdown --batch items.csv --out cd.md` 算降本总额与降本率。
3. 把结果回填「采购KPI看板」，并在「合规声明」「审批授权矩阵」补审批与利益冲突记录。
4. 高集中（HHI>0.25 或单源≥50%）须制定开发备选源计划。

**输出**：套用「采购KPI看板」+「审批授权矩阵」+「合规声明」；结论须含 HHI、单源占比、降本金额，超标项须标红并给改进方向。

---

## 工作流十一：数据接入适配（离线）

**目标**：本技能是**离线辅助工具**，不直连 ERP/SRM/EDI。本工作流提供「导入映射规范 + 适配器脚本」，把各系统导出数据标准化为内部字段，让其他脚本（po_tracker / kpi_dashboard / risk_monitor）能直接消费——这是技能从「手工填报」走向「数据驱动」的桥。

**映射口径**（对应模板「数据接入适配」）：
- 内部字段：po_no、supplier、material、qty、unit_price、po_amount、plan_date、actual_date、recv_qty、ok_qty、invoice_amount、credit_code。
- 导出列名 → 内部字段模糊匹配（如「采购订单号/订单号/PO_NO」均映射 po_no）；po_amount 缺失时自动按 qty×unit_price 派生。

**步骤**：
1. 从 ERP/SRM/WMS/QMS 导出 CSV/JSON。
2. `python import_adapter.py validate --file erp.csv` 校验必填列（po_no/supplier/qty）。
3. `python import_adapter.py normalize --file erp.csv --out std.csv` 标准化为内部格式。
4. 把 std.csv 喂给 po_tracker / kpi_dashboard 等脚本继续跑闭环。

**输出**：标准化 CSV + 校验结论（缺失字段则报错）; 未识别列名会提示已忽略。

---

## 输出纪律（必须遵守）

- 任何评估/比价/准入结论，**必须套用模板表格**，禁止纯文字自由格式。
- 涉及分数必须给出**打分依据**（来自数据或核查结果），不写空泛结论。
- 凡出现单源、高支出集中度（>30%）、或高风险项，必须显式标红预警。
- 风险核查结论必须标注**核查日期与信息来源**，陈旧信息（>90 天）需提示重新核查。
- 品类策略结论必须标注 **Kraljic 定位（战略 / 杠杆 / 瓶颈 / 非关键）** 与对应差异化策略，并据此决定后续准入/评估/比价的打法。
- 成本拆解结论必须给出 **Should-Cost 合计、供应商报价、差异率（红/黄/绿预警）**；差异率 ≥15% 须显式标红并要求供应商拆解报价或引入备选。
- 绩效分级结论必须给出 **每家等级、角色、建议份额（合计须≈100%）**；C/D 级须附 PIP 改进计划与闭环状态，单源（某家份额 >50%）须显式标红提示。
- 持续风险监控结论须标注 **每个供应商整体风险等级与最久未查天数**；「需重查(>90天)」项须显式标红并提示重查，高风险供应商准入一票否决。
- 采购执行闭环结论须给出 **三单匹配异常单数、延迟单数、平均不良率**；三单不匹配/延迟/高不良单须挂起付款并标红处理。
- 合规 KPI 结论须给出 **HHI 指数、最大供应商占比、年降本金额**；HHI>0.25 或单源占比≥50% 须标红并制定开发备选源计划。
- 数据接入须先经 `import_adapter.py` 标准化再喂给其他脚本；缺失必填字段（po_no/supplier/qty）的导入须报错拦截，不得带病入库。
